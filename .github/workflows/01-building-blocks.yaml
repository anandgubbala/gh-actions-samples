name: 01-Building-blocks
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Successful step
              run: |
                 echo "Succeeded"
                 exit 0
            - name: Say Good bye
              run: echo "Good Bye"